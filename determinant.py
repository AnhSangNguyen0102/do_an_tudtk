import matrix as mt
import gaussian_eliminate as ge


def determinant(self, A):       # Giải định thức của ma trận A (Dùng hàm gaussian_eliminate)
    return;                     # Trả về đáp số của định thức ma trận