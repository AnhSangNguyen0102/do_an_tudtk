import matrix as mt
import gaussian_eliminate as ge

def back_substitution(self, U, c): # Giải hệ tam giác, trả về vector x là vector kết quả
                                # của hệ phương trình (Dùng hàm gaussian_eliminate)
    return;                        